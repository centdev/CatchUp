<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usingintent.app" >

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegistrationActivity"
            android:label="Registration" >
            <intent-filter>
                <action android:name="andriod.intent.RegistrationActivity" />
                <category android:name="andriod.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SigninActivity"
            android:label="Sign-in" >
            <intent-filter>
                <action android:name="andriod.intent.SigninActivity" />
                <category android:name="andriod.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EventActivity"
            android:label="Events" >
            <intent-filter>
                <action android:name="andriod.intent.EventActivity" />
                <category android:name="andriod.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CreateEventActivity"
            android:label="Create Event" >
            <intent-filter>
                <action android:name="andriod.intent.CreateEventActivity" />
                <category android:name="andriod.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    <activity
        android:name=".DisplayEventActivity"
        android:label="Display Event" >
        <intent-filter>
            <action android:name="andriod.intent.DisplayEventActivity" />
            <category android:name="andriod.intent.category.DEFAULT" />
        </intent-filter>
    </activity>

    </application>

</manifest>
